# IR_MODEL
A lightweight Python-based information retrieval system that demonstrates document ranking using TF-IDF and Cosine Similarity. Built from scratch without external ML libraries, it features a Streamlit UI for real-time query input and retrieval of the most relevant documents.
